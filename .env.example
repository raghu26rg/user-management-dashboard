# Frontend API URL (used by Vite at build time)
# Local:
VITE_API_URL=http://localhost:3001/api

# Production (Vercel) — set this in Vercel dashboard after deploying the API:
# VITE_API_URL=https://YOUR-API-NAME.onrender.com/api
